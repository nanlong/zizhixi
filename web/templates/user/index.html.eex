<h2>Listing users</h2>

<table class="table">
  <thead>
    <tr>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for user <- @users do %>
    <tr>

      <td class="text-right">
        <%= link "Show", to: user_path(@conn, :show, user.username), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: user_path(@conn, :edit, user.username), class: "btn btn-default btn-xs" %>
        </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New user", to: user_path(@conn, :new) %>
