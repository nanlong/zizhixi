<h2>编辑分类</h2>

<%= render "form.html",
  changeset: @changeset,
  action: link_category_path(@conn, :update, @link_category),
  categories: @categories
%>

<%= link "Back", to: link_category_path(@conn, :index) %>
